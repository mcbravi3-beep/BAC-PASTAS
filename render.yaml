services:
  - type: web
    name: bac-pastas
    runtime: node
    plan: free
    branch: claude/pasta-factory-management-system-moecdp
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.22.2
